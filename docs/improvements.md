In budget add numerical value rather than low, medium, high
(Change the logic in the prompt as well)
Top K (1-10) is not required
we can fix amount and ratings of restaurants we are sending to the model, currently it's showing N/A, fix these